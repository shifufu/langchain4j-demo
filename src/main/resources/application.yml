spring:
  application:
    name: langchain4j-demo

server:
  port: 8080

# OpenAI / Volcengine Configuration
langchain4j:
  openai:
    api-key: ${OPENAI_API_KEY:xxxx}
    base-url: ${OPENAI_BASE_URL:https://ark.cn-beijing.volces.com/api/coding/v3}
    # 设置为 true 使用自定义火山引擎实现，false 使用标准 OpenAI 实现
    use-custom-volcengine: true
    chat:
      model: ark-code-latest
      temperature: 0.7
    embedding:
      # 使用与 chat 相同的模型
      model: ark-code-latest

# 日志配置
logging:
  level:
    com.example.ai: DEBUG
    dev.langchain4j: DEBUG
