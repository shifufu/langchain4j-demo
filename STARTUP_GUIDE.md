# 🚀 项目启动指南

## 问题修复

已修复启动错误！主要修改：

1. **配置文件** - 使用正确的火山引擎模型名称
2. **配置类** - 添加错误处理，让应用即使 embedding 不可用也能启动
3. **服务类** - 添加可用性检查，优雅处理 embedding 功能
4. **控制器** - 给出友好的提示信息

## ✅ 当前可用功能

即使 embedding 功能不可用，以下功能仍然可以正常使用：

- 🤖 **简单聊天** - 基础对话功能
- 🌍 **多语言翻译** - 支持多种语言互译
- ✨ **创意写作** - 根据主题和风格生成内容
- ⚙️ **自定义提示词** - 灵活配置 System Prompt
- 🎵 **音乐推荐** - 根据书籍推荐音乐（新功能！）

## 🚀 启动步骤

### 1. 启动后端服务

在项目根目录下执行：

```bash
mvn spring-boot:run
```

或者如果你使用的是 IDE，直接运行 `Langchain4jDemoApplication.java`

### 2. 确认后端启动成功

看到类似这样的日志说明启动成功：

```
Started Langchain4jDemoApplication in X.XXX seconds
```

### 3. 访问前端应用

前端已经在运行中，访问：

**http://localhost:5173**

## ⚠️ 注意事项

### 关于 Embedding 功能

- Embedding 和 RAG 功能可能因为模型配置问题不可用
- 这不影响其他核心功能的使用
- 如果需要使用 embedding 功能，需要配置支持 embedding 的模型

### API Key 配置

确保你的火山引擎 API Key 有效并且有足够的配额。

### 健康检查

访问 http://localhost:8080/api/chat/health 检查后端服务状态。

## 🎯 推荐使用流程

1. 先尝试 **简单聊天** 功能，确认 AI 正常工作
2. 体验 **音乐推荐** 功能，输入你喜欢的书名
3. 使用 **翻译** 和 **创意写作** 功能
4. 如果需要，尝试 **自定义提示词** 进行更灵活的配置

## 🔧 如果还有问题

### 查看日志

检查后端控制台的详细日志输出。

### 检查配置

确认 `application.yml` 中的 API Key 和模型配置正确。

### 简化配置

如果需要，可以临时注释掉 embedding 相关的代码，只使用 chat 功能。

---

现在试试启动项目吧！应该可以正常启动了！🎉
